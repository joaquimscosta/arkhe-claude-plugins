# Troubleshooting
- **Gemini key**: ensure `GEMINI_API_KEY` is set
- **Missing output**: check write permissions for `./output`
- **JSON errors**: validate region schema
- **Skill not triggering**: ensure triggers ('infographic', 'diagram') appear in user request
